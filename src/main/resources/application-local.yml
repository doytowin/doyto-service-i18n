logging:
  level:
    root: info
    org.springframework.security: debug

spring:
  application:
    name: @project.artifactId@-local
  cloud:
    config:
      enabled: false

eureka:
  client:
    register-with-eureka: false